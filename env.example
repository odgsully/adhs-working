# Copy to `.env` and fill in your own values.
MCAO_API_KEY=
RAW_DATA_DIR=./raw

# Optional — override defaults in adhs_etl.config.Settings
FUZZY_THRESHOLD=85      # RapidFuzz match threshold (0‑100)
GEOCODER_RETRIES=3      # Number of retries on external geocoder
DRY_RUN=false           # Force no‑write behaviour regardless of CLI flag
